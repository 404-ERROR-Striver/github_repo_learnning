text learning

the chnage has been made 

understanding the working of git and making my contributuion 
 change has to be amde in the index file of the project
 To work on this project sice exam is going on 
another day learning for data analytics so i am unaable to work on github 
another day learning for data analytics so i am unaable to work on github 
another day learning for data analytics so i am unaable to work on github 
another day learning for data analytics so i am unaable to work on github 

another day learning for data analytics so i am unaable to work on github 

another day learning for data analytics so i am unaable to work on github 

another day learning for data analytics so i am unaable to work on github 
import java.util.*;

class Solution {
    public String largestNumber(int[] nums) {
        String[] arr = new String[nums.length];

        for (int i = 0; i < nums.length; i++) {
            arr[i] = String.valueOf(nums[i]);
        }

        Arrays.sort(arr, (a, b) -> (b + a).compareTo(a + b));

        // edge case: if largest is "0", result is "0"
        if (arr[0].equals("0")) return "0";

        StringBuilder sb = new StringBuilder();
        for (String s : arr) {
            sb.append(s);
        }

        return sb.toString();
    }
}


another day learning for data analytics so i am unaable to work on github 
another day learning for data analytics so i am unaable to work on github 
another day learning for data analytics so i am unaable to work on github 
class Solution {
    public int uniquePaths(int m, int n) {
        int[][] dp = new int[m][n];

        // First row and first column
        for (int i = 0; i < m; i++) dp[i][0] = 1;
        for (int j = 0; j < n; j++) dp[0][j] = 1;

        // Fill DP table
        for (int i = 1; i < m; i++) {
            for (int j = 1; j < n; j++) {
                dp[i][j] = dp[i - 1][j] + dp[i][j - 1];
            }
        }

        return dp[m - 1][n - 1];
    }
}

    public int uniquePaths(int m, int n) {
        int[][] dp = new int[m][n];

        // First row and first column
        for (int i = 0; i < m; i++) dp[i][0] = 1;
        for (int j = 0; j < n; j++) dp[0][j] = 1;

        // Fill DP table
        for (int i = 1; i < m; i++) {
            for (int j = 1; j < n; j++) {
                dp[i][j] = dp[i - 1][j] + dp[i][j - 1];
            }
        }

        return dp[m - 1][n - 1];
    }
}
    public int uniquePaths(int m, int n) {
        int[][] dp = new int[m][n];

        // First row and first column
        for (int i = 0; i < m; i++) dp[i][0] = 1;
        for (int j = 0; j < n; j++) dp[0][j] = 1;

        // Fill DP table
        for (int i = 1; i < m; i++) {
            for (int j = 1; j < n; j++) {
                dp[i][j] = dp[i - 1][j] + dp[i][j - 1];
            }
        }

        return dp[m - 1][n - 1];
    }
}
